# Database Folder

The executable backend remains at `D:\Spring-Workspace\property-registration-backend` because Spring Tools/Eclipse is configured to run it from that workspace.

Database setup, entity descriptions, reset notes and production migration guidance are documented in:

```text
D:\PropertyRegistrationSystem\documentation\DATABASE.md
```

Local database:

```text
MySQL localhost:3307
Database property_registration_db
```
